# Do not place AI provider secrets in an Android project.
#
# Gemini and other provider credentials must be stored only in an authenticated backend.
# The Android client should call that backend with a user session token.
